import React from 'react';
import {render, screen, fireEvent} from '@testing-library/react';
import GeneratorStep2 from '../components/generatorS``.js';

/*
describe('ModeBtn Component' , () => {

    test('update the state of mode to PO or PR on button click')
    //Render the component
    render(<GeneratorStep2/>)

    //Find the state

    //Find the button and click it
    const poButtonElement = screen.getByRole('radio', {name: 'PO'});
    const prButtonElement = screen.getByRole('radio', {name: 'PR'});
    fireEvent.click(poButtonElement);
    fireEvent.click(prButtonElement);

    //Assert the state has changed after click the button
    experimental_useEffectEvent()
})
*/